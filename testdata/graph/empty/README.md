A directory holding no entity file, which loads as an empty graph rather than as
a failure. README.md is here on purpose: a directory walk reads only files whose
extension is `.dfc`, so this file makes the directory exist in git without
making it a model.
