; The definitions which come first. What collides with them is in the file
; beside this one, because an id is unique across the whole model rather than
; within a file.
(vertex geom:V-01 (frame frame:building))

(vertex geom:V-02 (frame frame:building))

(edge geom:E-01 (frame frame:building) (vertices geom:V-01 geom:V-02))
